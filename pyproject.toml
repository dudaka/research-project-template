[project]
name = "{{project-name}}"
version = "0.1.0"
description = "{{One-line description}}"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.3",
    "scipy>=1.17.1",
    "matplotlib>=3.10.8",
    "python-dotenv>=1.2.2",
]
